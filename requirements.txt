# ---- PyTorch CPU (doit être en premier, sans index) ----
torch==2.4.1+cpu
torchaudio==2.4.1+cpu   # optionnel, on ne l’utilise pas mais évite les erreurs
torchvision==0.19.1+cpu # optionnel

# ---- Les autres paquets (installés depuis PyPI normal) ----
streamlit
transformers
datasets
accelerate